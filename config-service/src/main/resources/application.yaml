server:
  port: 8081
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/BrancoGarciaS/Tingeso_Evaluacion2
          search-paths: "/config-data"